# 🔄 Gemini CLI 核心概念与开发闭环详解

本指南将带你深入理解 Gemini CLI 的底层设计哲学与核心技术概念，并为你展示一个标准的自动化「开发闭环」实例。

---

## 🏗️ 核心概念详解

### 1. ReAct (Reasoning and Acting) 协同范式
传统的聊天 AI 只能进行**“思考 (Reasoning)”**和**“输出文本”**。而 Gemini CLI 采用的是 **ReAct 架构**，它将思考与行动紧密结合：
* **思考 (Reasoning)**：分析你输入的自然语言，拆解出任务的真实意图（例如：“用户想修复一个 Bug，我需要先找到报错的代码位置”）。
* **行动 (Act)**：从工具箱中挑选最合适的工具执行（如运行 `grep_search` 精准检索，或运行 `read_file` 读取代码）。
* **观察 (Observation)**：工具执行后，AI 会查看返回的真实 system 结果（如报错日志、代码上下文），并基于这些新信息进入下一轮的“思考 -> 行动”闭环。

### 2. Context Window (上下文窗口) 与 Token 效率
上下文窗口是 AI 在当前会话中能够“同时记住”的最大信息量。Gemini 拥有极其庞大的上下文窗口，但**每一次对话，整个历史记录（包括你发的消息、我写的代码、工具返回的几百行日志）都会被重新发送给大模型。**
* **高效规范**：如果单次读取了太多无关的大文件，会迅速堆积 Token，导致后续的对话变慢、甚至超出限制。这也是为什么我更倾向于使用 `grep_search` 精准定位，或者局部读取大文件，而不是动辄“全盘读取”。

### 3. Contextual Precedence (上下文优先级：GEMINI.md)
在软件工程中，没有绝对统一的“正确代码”，只有“符合当前项目规范的代码”。
* **机制**：Gemini CLI 在每次启动或执行任务时，会**自动、隐式地将项目根目录下的 `GEMINI.md` 文件内容置于最高优先级**。
* **应用**：它相当于赋予了 AI 一个“项目级人设”。如果你的 `GEMINI.md` 规定了“禁止使用 `any` 类型”，那么即使你在具体提问时没有强调，我在为你编写代码时也绝对不会使用 `any`。

### 4. Inquiry (问询) vs. Directive (指令)
这是 Gemini CLI 区分“只动口”和“动手”的黄金标准：
* **Inquiry (问询)**：探讨可能性、请求分析、索要建议或解释。AI 将**只进行分析和设计**，输出技术报告，绝对不会修改任何本地 file 或运行改动命令（例如：*“帮我看看这段代码有什么优化空间？”*）。
* **Directive (指令)**：包含明确的、无歧义的动作指示。AI 将**直接进入执行阶段**，调用读写和 Shell 工具，对项目进行外科手术式的修改（例如：*“帮我把这个 Bug 修复了。”*）。

### 5. Empirical Reproduction (故障的经验主义重现)
在软件工程中，盲目修 Bug 往往会引入新的 Bug。Gemini CLI 遵循**“先证明，后修复”**的原则：
* 当收到修 Bug 的指令时，AI 的第一步通常不是去改业务代码，而是去编写一个能够稳定触发该 Bug 的**测试用例（Reproduction Script）**。只有当测试用例确实失败了，才证明找到了病灶；修完代码后测试通过，才证明真正根治了问题。

### 6. MCP (Model Context Protocol - 模型上下文协议)
* **定义**：连接大模型能力与本地/云端生态的**开放式通用接口**。
* **通俗理解**：AI 默认只有通用的逻辑思维。MCP 就像是给 AI 插入的“扩展卡槽”。通过接入不同的 MCP 服务器（比如 Git MCP），大模型就能瞬间获得操作 Git、连接数据库、或者控制浏览器的专用、结构化卡槽工具，而无需编写复杂的适配代码。

---

## 📊 实例演示：标准的「开发闭环 (Development Lifecycle)」

以下为你完整重现一个「统计当前项目里所有 .md 文件的字数」的任务流，展示 AI 内部如何通过 **研究 -> 策略 -> 执行 -> 验证** 的闭环独立解决问题：

### 🔍 Phase 1. 研究阶段 (Research)
AI 首先调用 `list_directory` 工具查看当前工作目录。通过观察，发现目录中包含三个新创建的 `.md` 文件：`GEMINI_GUIDE.md`、`GEMINI_COMMANDS.md` 和 `GEMINI.md`，证实了可以安全地针对它们运行字数统计。

### 📋 Phase 2. 策略阶段 (Strategy)
根据研究结果，AI 制定了一个精确的逻辑方案：
1. 编写一个 Python 脚本 (`count_words.py`)。
2. 考虑到文档包含中英文，使用正则（Regex）分别精准匹配**中文字符**与**英文单词/数字**。
3. 自动扫描当前目录下所有的 `.md` 格式文件，并漂亮地格式化打印出各自的字数和最终汇总。

### 🛠️ Phase 3. 执行阶段 (Act)
AI 使用 `write_file` 工具将构思好的 Python 脚本精准写入到了本地磁盘中。

### ✅ Phase 4. 验证阶段 (Validate)
代码交付完成并不意味着结束。AI 立即通过 `run_shell_command` 运行了刚刚创建的脚本来检验其功能，最终在控制台完美打印输出当前文档的统计汇总，从而顺利交付任务。

---

🏁 *恭喜！你现在已经掌握了配合 Gemini CLI 高效开发的底层逻辑。开启你的生产力之旅吧！*