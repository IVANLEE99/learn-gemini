# Gemini CLI 多模态与图片上传指南

Gemini CLI 具备强大的**多模态视觉能力**。你不需要像在网页端那样通过点击按钮来“上传”图片，而是可以直接通过**路径引用**的方式让 AI 读取和分析本地的图片。

## 1. 使用 `@` 符号引用本地图片 (推荐)
在交互式终端中输入 Prompt 时，你可以使用 `@` 符号加上图片在本地的路径。CLI 会自动读取该图片并作为多模态上下文发送给模型。

**示例：**
* 询问图片内容：
  `请帮我看看这张架构图里画了什么？ @./assets/architecture.png`
* 提取报错信息：
  `帮我识别并解决这个报错截图 @/Users/admin/Desktop/error.jpg`

## 2. 剪贴板直接粘贴
如果你的终端模拟器（Terminal Emulator）支持富文本粘贴，你可以：
1. 截一张图（或复制一张图片到系统剪贴板）。
2. 在 Gemini CLI 的输入框中直接按下 `Ctrl+V` (Windows/Linux) 或 `Cmd+V` (Mac)。
3. CLI 会自动侦测到剪贴板里的图像数据，将其存为临时文件，并附加到当前的 Prompt 中。

## 3. 批量处理图片
你甚至可以直接将一个包含图片的**文件夹路径**丢给 Gemini，并附上任务指令。它会自动遍历并读取里面的视觉内容。

**示例：**
* `根据 ./photos 目录下每张照片的内容，自动将它们重命名。`
* `分析 ./screenshots 里的所有 UI 截图，并给我生成对应的 TailwindCSS 布局代码。`

## 4. 支持的图片格式
目前，Gemini CLI 稳定支持标准的常见图片格式，包括：
* **PNG** (`.png`)
* **JPEG** (`.jpg`, `.jpeg`)
* **WebP** (`.webp`)

> **💡 模型选择建议：**
> 处理图片时，建议使用默认的 `Flash` 模型以获得最快的处理速度。但如果你需要分析极其复杂的流程图、高分辨率的架构图或是包含极小文字的报错截图，建议输入 `/model` 并切换到逻辑推理能力更强的 **`Pro`模型**。