# Gemini CLI 回退与撤销指南 (Rewind)

在使用 Gemini CLI 进行开发时，如果 AI 的修改不符合预期，你可以轻松地回退这些更改。Gemini 提供了一个类似于“全局 Ctrl+Z”的强大功能：**Rewind (时光倒流)**。

## 1. 如何触发 Rewind？
在交互式终端中，你可以通过以下两种方式呼出回退菜单：
* **快捷键:** 在输入框为空时，**连按两次 `Esc` 键**。
* **命令:** 在输入框中输入 `/rewind` 并回车。

## 2. 选择恢复点
触发后，CLI 会弹出一个交互式列表，显示了你最近的对话和 AI 的操作记录。你可以使用方向键上下移动，选择你想“穿越”回到的那个时间点。

## 3. 三种回退模式
选中恢复点后，系统会让你选择回退的范围：

* **⏪ Rewind Both (回退全部):** 最常用的模式。同时撤销 AI 对**文件系统的修改**，并清除该时间点之后的**对话记忆**。就像这一切从未发生过。
* **📁 Revert Code Changes (仅撤销代码):** 撤销 AI 对文件的修改，但是**保留对话历史**。适用于：你发现 AI 的思路是对的，但代码写崩了，你想保留上下文让它换种方式重写。
* **💬 Rewind Conversation (仅回退对话):** 保留 AI 修改的文件，但**清除对话历史**。适用于：代码改得很完美，但之前试错的聊天记录太长了，你想清理掉以节省 Token。

## ⚠️ 注意事项与局限性
1. **仅限 AI 操作:** Rewind 功能**只能撤销 Gemini AI 自己使用工具（如 `replace`, `write_file`）所做的修改**。它无法撤销你自己手动修改的文件，或者通过运行 Shell 脚本（如 `! git reset`）产生的副作用。
2. **Git 是最强后盾:** 虽然 Rewind 很好用，但在进行大规模重构前，依然强烈建议你在本地先执行 `git commit`。如果 Rewind 无法满足需求（比如撤销了通过 Shell 执行的命令），你随时可以使用终端命令如 `git reset --hard` 或 `git checkout .` 来恢复代码。