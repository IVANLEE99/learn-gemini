# 🚀 Gemini CLI 新手指南

欢迎使用 **Gemini CLI**！我是你的终端 AI 结对编程伙伴（Pair Programmer）。与传统的聊天 AI 不同，我不仅能回答问题，还能在你的授权下**直接阅读、分析、修改本地代码库**，并执行终端命令。

本指南将帮助你快速掌握我的核心概念和高效使用技巧。

---

## 🛠️ 核心操作模式：ReAct

我采用 **ReAct (Reason and Act)** 运行机制：
1. **思考 (Reason):** 分析你的意图，规划解决路径。
2. **行动 (Act):** 挑选并调用合适的工具（如读写文件、运行 Shell 命令、联网搜索等）。
3. **验证 (Validate):** 检查行动结果（如运行测试、检查语法），确保交付的代码完全可用。

> ⚠️ **安全提示**：涉及到**修改文件系统**或**执行系统命令**的操作，终端都会弹出**确认提示**。只有在你按下回车授权后，我才会真正执行，确保你的系统绝对安全。

---

## 📱 常用斜杠命令 (Slash Commands)

在终端交互中，你可以随时键入以下命令来管理会话：

* `/help`：显示帮助信息和所有可用命令。
* `/model`：调出菜单，在不同的 Gemini 模型（如 Flash 或 Pro）之间进行切换。
* `/tools`：查看当前我被赋予了哪些工具权限（如 `read_file`, `run_shell_command` 等）。
* `/memory`：查看或管理我为你记录的个性化或项目专属上下文记忆。
* `/clear`：清除当前会话历史，刷新上下文。

---

## 🎯 新手推荐探索指令

你可以像和真人资深工程师沟通一样向我下达指令。以下是一些经典的软件工程场景，不妨复制到终端直接试一试：

### 1. 架构探索与代码理解
> "探索当前目录，帮我分析一下这个项目的整体架构、核心模块和技术栈。"

### 2. 代码重构与差错排查
> "帮我看看核心文件里面有没有潜在的 Bug 或性能隐患？如果有，请给出修复方案。"

### 3. 自动化测试编写
> "为制定函数补全单元测试，并尝试使用项目已有的测试框架运行它们。"

### 4. 联网搜索最新技术文档
我有内置的 Google Search 能力。当你遇到最新的框架报错或 API 变更时：
> "查一下最新版本的标准异步调用 API 是什么？并给出一个 Demo。"

---

## 🎛️ 进阶技巧：使用 `GEMINI.md` 定制我

如果你希望我在当前项目里始终遵守某些特定的工程规范，可以在项目根目录下创建一个 **`GEMINI.md`** 文件（例如：规定“必须使用 TypeScript 严格模式”、“变量命名采用小驼峰”、“提交代码前必须运行 linter”）。

我会**自动读取**该文件中的团队规范，并在后续的所有对话、代码编写和重构中严格执行。

---

## 💡 开启你的第一个任务

现在，请在终端直接输入你的第一个指令！你可以尝试：
* `帮我检查一下当前目录下的文件结构。`
* `我想用 Python/Node.js 写一个新工具，能帮我规划一下吗？`