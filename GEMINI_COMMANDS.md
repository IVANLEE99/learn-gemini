# 📱 Gemini CLI 斜杠命令 (Slash Commands) 使用说明

在 Gemini CLI 交互终端中，你可以通过键入 **斜杠 `/`** 开头的命令来快速管理 AI 会话状态、切换模型、查看工具或配置记忆。以下是所有可用命令的详细使用说明：

---

### 1. `/help` - 获取帮助
* **功能描述**：显示终端的帮助菜单，列出所有当前可用的斜杠命令及其简要说明。
* **使用场景**：忘记某个特定命令的拼写或想查看 CLI 的最新支持功能时。

### 2. `/model` - 切换 AI 模型
* **功能描述**：调出模型选择菜单，允许你在不同的 Gemini 模型之间进行实时切换（例如从高速度的 `Gemini Flash` 切换到高逻辑能力的 `Gemini Pro`）。
* **使用场景**：
  * 执行日常的文件浏览、简单问答时，使用默认模型以保持极速响应。
  * 遇到复杂的架构重构、深度的 Bug 根因分析或编写跨模块测试时，使用此命令切换到更强大的高级模型。

### 3. `/tools` - 查看工具箱
* **功能描述**：列出当前会话中 AI 被赋予的所有底层工具权限（例如 `read_file`, `write_file`, `run_shell_command`, `google_web_search` 等）。
* **使用场景**：想确认 AI 能否执行某项具体操作（如是否可以联网搜索、是否可以运行本地 shell）时。

### 4. `/memory` - 记忆库管理
* **功能描述**：查看、更新或清理 AI 记录的个性化或项目专属上下文记忆（包括本地项目记忆与全局个人偏好）。
* **使用场景**：
  * 检查 AI 是否记住了你的特殊编码习惯。
  * 当项目配置发生变更，需要手动刷新或删除陈旧的局部记忆时。

### 5. `/mcp` - 模型上下文协议管理
* **功能描述**：查看和管理集成的 MCP (Model Context Protocol) 服务器状态。MCP 允许你将自定义的外部上下文、工具或数据源接入到当前会话中。
* **使用场景**：配置了第三方开发工具链插件，需要调试或检查其连接状态时。

### 6. `/clear` - 刷新会话上下文
* **功能描述**：清空当前终端的对话历史，重置 AI 的上下文窗口。
* **使用场景**：当一个复杂的任务已经彻底结束，准备开启一个完全无关的新任务时。使用 `/clear` 可以有效防止历史对话干扰新任务的判断，同时能大幅提高响应速度。

### 7. `/bug` - 反馈与报错
* **功能描述**：直接向官方团队提交当前 CLI 工具的使用 Bug 或产品改进建议。
* **使用场景**：在运行 CLI 过程中遇到非预期的程序崩溃、工具调用异常或功能失效时。

---

> 💡 **快速使用技巧**：在终端中输入 `/` 字符后，可以直接配合键盘的 `Tab` 键或上下方向键进行命令自动补全与选择。