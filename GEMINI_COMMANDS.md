# 📱 Gemini CLI 斜杠命令 (Slash Commands) 全面使用指南

在 Gemini CLI 交互终端中，你可以通过键入 **斜杠 `/`** 开头的命令来快速管理 AI 会话状态、切换模型、查看工具或配置记忆。

经过详细梳理，Gemini CLI 当前总共提供 **42 个（含别名）** 斜杠命令。以下是完整列表及详细说明：

---

### 一、 基础交互与帮助 (5 个)
* **`/help`** - 显示终端的帮助菜单，列出常用斜杠命令及其用法。
* **`/?`** - `/help` 的别名命令，功能完全相同。
* **`/about`** - 显示当前 CLI 的版本信息、运行环境等，在反馈 Bug 时非常有用。
* **`/docs`** - 自动在浏览器中打开 Gemini CLI 的官方在线文档。
* **`/bug`** - 直接提交 GitHub Issue，反馈 CLI 工具的缺陷或产品改进建议。

### 二、 会话与状态管理 (8 个)
* **`/clear`** - 清空当前终端的屏幕和对话滚动历史，彻底重置 AI 的上下文窗口。
* **`/quit`** - 退出 CLI 应用程序。可附加 `--delete` 参数来擦除当前会话的临时文件和历史记录。
* **`/exit`** - `/quit` 的别名命令，功能完全相同。
* **`/chat`** - 打开交互式的会话浏览器，或使用子命令（如 `save <tag>`, `resume <tag>`, `list`, `share`）来手动存档、分享或恢复上下文。
* **`/resume`** - `/chat` 的别名命令，功能和子命令完全相同。
* **`/compress`** - 对当前极其冗长的对话记录进行“压缩与总结”，在保留关键信息的前提下大幅节省 Token 消耗。
* **`/plan`** - 切换到“计划模式 (Plan Mode)”（纯思考、只读模式）。这允许你在让 AI 动手写代码之前，先仔细审阅并调整它生成的任务计划。
* **`/stats`** - 显示当前会话、模型使用和各个工具调用的详细 Token 消耗及耗时统计。

### 三、 撤销与代码回退 (2 个)
* **`/rewind`** - 强大的“时光倒流”功能。向上游溯源对话历史，以交互方式让你选择撤销代码修改、清除废弃对话，或者两者同时执行。
* **`/restore`** - 将项目内的文件精准恢复到某一次特定工具执行之前的状态（更底层的状态恢复）。

### 四、 配置与记忆管理 (7 个)
* **`/settings`** - 打开交互式编辑器，修改全局（用户级）或项目级的 `.gemini/settings.json` 系统配置。
* **`/model`** - 调出模型选择菜单，允许你在不同的底层大语言模型（如 Gemini Flash、Pro 等）之间实时切换。
* **`/memory`** - 查看、更新或清理 AI 从你的 `GEMINI.md` 及私有 `MEMORY.md` 中提取和汇总的上下文记忆库。
* **`/init`** - 全局分析当前项目代码库，自动为你生成一份量身定制的 `GEMINI.md` 项目级研发规范文件。
* **`/directory`** - 管理多目录工作区（Workspace）。你可以使用该命令将多个项目文件夹加入到同一个 AI 上下文中。
* **`/dir`** - `/directory` 的别名命令，功能完全相同。
* **`/policies`** - 列出系统当前运行模式下，所有已激活的安全策略（如文件访问限制、网络请求白名单）。

### 五、 高级功能与扩展 (7 个)
* **`/tools`** - 列出当前会话中 AI 能够调用的所有底层工具列表（如读写文件、网络搜索等）。可使用 `desc` 查看详情或 `nodesc` 仅看名称。
* **`/mcp`** - 管理 Model Context Protocol (MCP) 服务器集成，例如配置连接 GitHub 或本地数据库的插件服务器状态。
* **`/skills`** - 管理 Agent Skills（专门的技能知识包），支持 `enable`, `disable`, `reload`, `list` 等子操作。
* **`/extensions`** - 管理针对 CLI 的第三方插件和扩展程序，支持 `install`, `uninstall`, `list`, `update` 等。
* **`/agents`** - 查看和手动调用系统内可用的本地或远程子代理引擎（例如 `codebase_investigator` 等专用代理）。
* **`/commands`** - 查看和管理开发者通过本地 `.toml` 配置文件自定义加载的专属斜杠命令。
* **`/hooks`** - 管理用于拦截和修改 CLI 工作流或生命周期行为的 Hook 钩子。

### 六、 终端与界面个性化 (8 个)
* **`/theme`** - 打开交互对话框，更改 CLI 终端界面的整体视觉颜色和主题。
* **`/vim`** - 一键切换，在文本输入区启用或禁用纯正的 Vim 风格按键绑定（Normal/Insert/Visual 模式）。
* **`/copy`** - 快速将 AI 上一次回复的文本（或生成的代码块）提取并复制到操作系统的剪贴板。
* **`/shells`** - 管理、切换或查看那些正由 AI 在后台长驻运行的 Shell 进程（例如监听构建服务或启动的测试服务器）。
* **`/bashes`** - `/shells` 的别名命令，功能完全相同。
* **`/ide`** - 查看和管理当前终端与外部 IDE (如 VSCode 等) 配套辅助插件的连接状态。
* **`/editor`** - 选择并配置当 CLI 需要让你输入大量文本（如编写长 prompt）时所调用的外部代码编辑器。
* **`/terminal-setup`** - 专用于在特定的外部 IDE 集成终端中配置多行输入的快捷键修正方案。

### 七、 其他系统级操作 (5 个)
* **`/auth`** - 重新打开鉴权对话框，以修改或切换登录方式（例如从 API Key 切换为 OAuth）。
* **`/permissions`** - 精细化管理每个文件夹的系统信任状态和读写安全沙盒隔离级别。
* **`/privacy`** - 显示详细的隐私数据收集声明，并提供修改遥测数据收集同意书的选项。
* **`/upgrade`** - 自动在浏览器中打开升级页面，引导你升级 Gemini Code Assist 的服务使用层级。
* **`/setup-github`** - 自动化脚手架，用于在当前代码仓库快速生成 Issue 自动分发和 PR 智能审查的 GitHub Actions 配置文件。

---

> 💡 **快捷键提示**：在提示符处输入 `/`，随后按 `Tab` 键即可唤出所有可用命令的智能补全菜单。对于任何命令，直接执行即可在终端内看到该命令的具体参数提示。