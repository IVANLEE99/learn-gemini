# 🌌 Gemini CLI 高阶概念与架构全景指南

本指南将带你深度剖析 Gemini CLI 生态系统中的核心高阶概念。理解这些机制能让你在面对复杂重构、大型协作及深度定制需求时游刃有余。

---

## 🛠️ 核心架构与能力扩展

### 1. Agent (智能体 / 子智能体)
Gemini CLI 采用的是**主从多智能体架构**。你当前正在直接对话的是「主智能体」，它扮演着 **Strategic Orchestrator (战略协调者)** 的角色。为了维持极高的上下文效率，主智能体会将特定类型的繁重任务分包给内置的**「Sub-Agents (子智能体)」**：
* **`codebase_investigator`**：专精于跨模块的代码分析、依赖树梳理和全局 Bug 根因定位。
* **`generalist`**：通用型强力子智能体，适合执行高数据量的批量改动、繁琐重构，以此保持主会话历史的极度精简。

### 2. Tool (工具)
Tool 是 AI 作用于物理世界的「器官」。AI 本身只具备逻辑推理能力，而工具箱赋予了它实际的行动力。工具分为两大类：
* **原子级工具**：由系统内置，提供精准、微观的操作。例如 `replace`（外科手术式局部文本替换，比直接重写整个文件更节省 Token）、`grep_search`（正则检索）以及 `run_shell_command`（执行终端指令）。
* **高阶扩展工具**：通过协议外接的复合工具箱。

### 3. MCP (Model Context Protocol - 模型上下文协议)
MCP 是一种开放的标准协议，它是**连接大模型思维与外部生态的通用卡槽**。
* **机制**：通过 MCP，第三方开发者可以编写独立的“MCP 服务器”，将其私有数据、API 或特殊工具封装并暴露给 Gemini CLI。例如，接入 `Git MCP Server` 后，AI 将不再通过粗暴的 Shell 拼接运行 `git commit`，而是直接通过底层标准的、带有强类型校验的原子接口直接操作 Git。

### 4. Skill (技能)
Skill 是**针对特定垂直领域沉淀的专家级工作流规范与指令集**。
* **机制**：当激活某个技能（如使用 `activate_skill` 触发 `skill-creator`）时，系统会隐式加载一份局部的 `<instructions>`（专家向导）。在接下来的一段时间内，该技能的规则将**覆盖默认的通用逻辑**，强迫 AI 严格按照行业最顶尖的 SOP 步骤去执行特定任务。

---

## 🧠 动态上下文与记忆机制

### 5. Memory (分级记忆系统)
为了防止记忆混乱或隐私泄漏，Gemini CLI 引入了严格的**三级隔离记忆网络**：
* **Tier 1: 团队共享层 (`./GEMINI.md`)**：存放项目级公共规范。随代码库一起提交（Commit）并共享给团队，所有人可见。
* **Tier 2: 本地私有层 (`~/learn-gemini/memory/MEMORY.md`)**：存放个人 machine-specific、只属于此仓库的局部私人笔记（如本地特殊的 DB 端口）。**绝对不会被 Git 提交**。
* **Tier 3: 全局偏好层 (`~/.gemini/GEMINI.md`)**：跨项目跟随。记录你的通用代码偏好（例如：“我始终偏好用 Vanilla CSS 替代 Tailwind”）。

### 6. Hook Context (挂钩上下文)
Hook 是一种**自动化的被动数据感知通道**。
* **机制**：由外部系统钩子（如 Git Pre-commit、CI 运行时或系统监控）自动触发，并将其产生的数据封装在 `<hook_context>` 标签中隐式注入给 AI。AI 会将其视作**只读的客观事实**，在回答你时作为背景参考，但绝不会被 Hook 逆向绑架去执行非法操作。

### 7. Settings (配置系统)
Settings 掌控着 CLI 的运行时底座特性，包括全局的模型映射路由、 Approval Mode（安全确认机制的触发阈值）、环境隔离策略以及默认的数据截断长度。可以通过系统的配置文件进行深度调优。

---

## 📊 深度协同流

### 8. Plan Mode (设计蓝图模式)
对于直观的修复或提问，AI 会在常规模式下立即执行；而面对**大范围跨模块重构、全新应用的从零构建、或者高风险的底层改动**时，必须使用 `enter_plan_mode` 强行切入「蓝图设计模式」。
* **运行机制**：在 Plan Mode 下，**所有会修改文件系统或执行 Shell 的敏感写入工具将被暂时完全锁定**。AI 会强制退回到纯粹的「研究与设计沙盒」中，与你反复探讨并输出完整的技术规格书（Specs）。只有在设计蓝图获得你的**显式确认（Approval）**后，AI 才会退出 Plan Mode 并将蓝图拆解为具体的 Directive 去安全执行。