# Gemini CLI 配置与记忆管理指南

Gemini CLI 的配置主要分为两类：**系统与功能配置** 和 **行为与规范配置**。它们各自有不同的存储位置和优先级。

---

## 优先级原则
优先级通常遵循 **“就近原则”**：当前目录/项目级别的配置会覆盖或合并全局级别的配置。

---

## 一、 系统与功能配置 (`settings.json`)
主要用于配置 MCP 服务器、模型偏好、UI 主题、安全验证等。

**优先级排序（由高到低）：**

1. **项目级配置 (Project Level)**: `当前项目目录/.gemini/settings.json`
   * **最高优先级**。如果你希望某个 MCP 插件（比如 GitHub 插件）或特定的 API 密钥仅在当前项目中生效，可以将其放置在此处。
2. **全局级配置 (Global Level)**: `~/.gemini/settings.json` (用户主目录下)
   * **后备优先级**。这是 Gemini CLI 的默认系统级配置文件。如果你在项目里没有特殊配置，CLI 会默认读取这里的设置。

*(注：项目级配置会与全局配置进行“深度合并”，如果遇到相同的键名，项目级配置会覆盖全局配置。)*

---

## 二、 行为与规范配置 (`GEMINI.md` & `MEMORY.md`)
主要用于指导 Gemini AI 的行为方式、编码规范、架构准则和个人偏好。

**优先级排序（由高到低，即作用域由窄到宽）：**

1. **子目录规范 (Subdirectory Instructions)**: `项目子目录/GEMINI.md` (例如 `src/GEMINI.md`)
   * **最高优先级**。专门针对某个特定模块或组件的规则（例如前端文件夹和后端文件夹可以有不同的规范）。
2. **项目级共享规范 (Project Instructions)**: `项目根目录/GEMINI.md`
   * **核心项目规则**。通常提交到 Git 仓库，团队共享。里面包含了该项目的架构、核心库使用指南和整体风格。这里的指令具有强制约束力。
3. **项目专属私有记忆 (Private Project Memory)**: `~/.gemini/tmp/<项目名>/memory/MEMORY.md`
   * **你的私人笔记**。不提交到 Git。记录你个人的本地开发环境设置、针对这个项目的临时调试技巧或私人工作流。
4. **全局个人偏好 (Global Personal Memory)**: `~/.gemini/GEMINI.md`
   * **通用底线规则**。跨项目生效的个人习惯（例如：“我所有的项目都偏好使用 pnpm 而不是 npm”）。它会在所有会话中自动加载，但如果与具体项目的 `GEMINI.md` 冲突，会优先服从项目的要求。

---

## 三、 `GEMINI.md` 需要手动维护吗？

对于 `GEMINI.md` 及其相关的配置文件，**既可以完全手动管理，也可以通过自然语言交给我自动维护（推荐做法）**。

### 1. 自动添加（推荐：通过对话让 AI 代劳）
作为你的 AI 助手，我已经内置了维护配置文件的权限和逻辑。在日常开发中，你可以直接向我下达指令，我会自动判断该写到哪个文件里，并使用工具替你创建或修改文件。

**使用示例：**
* **项目规范：** *“在这个项目中，请帮我记住：所有 Python 缩进必须使用 4 个空格，且不要使用 type hints。”* 👉 我会自动修改项目根目录的 `GEMINI.md`。
* **个人偏好：** *“在我所有的项目里，请始终默认使用 Vim 的操作习惯。”* 👉 我会自动将其写入你电脑的全局偏好文件 `~/.gemini/GEMINI.md` 中。
* **本地私有记忆：** *“记住我这台电脑的本地开发数据库端口是 5432。”* 👉 我会自动将其写入不提交至 Git 的私有记忆区 `MEMORY.md`。

> 💡 **提示**：我没有所谓的隐藏“记忆保存工具”，我是通过普通的“读写文件”工具直接编辑这些 Markdown 文档的。你可以随时用编辑器打开它们审查我写的内容。

### 2. 手动维护（直接编辑文件）
如果你更喜欢掌控一切，或者你想一次性导入团队已有的开发规范，你完全可以直接在 IDE 中手动创建和修改 `GEMINI.md`。每次对话时，**Gemini CLI 会自动读取它们**，不需要执行额外的“加载”命令。

### 总结建议
**手动初始化核心的团队规范，后续开发中产生的碎片化规则或个人习惯，直接在终端中吩咐我记录即可。**